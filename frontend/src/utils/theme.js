import { ROOM_LABEL } from './helpers'

export const appMeta = {
  title: `${ROOM_LABEL}预约系统`,
  shortTitle: ROOM_LABEL,
  bookingPageTitle: `${ROOM_LABEL}预约`,
  adminPageTitle: `${ROOM_LABEL}管理后台`
}
